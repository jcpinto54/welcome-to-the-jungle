{
  "name": "jungle-wizard",
  "version": "0.1.0",
  "private": true,
  "description": "Jungle Wizard: a PS1-style 3D rhythm action-RPG in the browser. Your spells are the drums.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "test:e2e": "node --test --test-concurrency=1 \"tests/e2e/*.test.cjs\"",
    "build:artifact": "node tools/build-artifact.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
